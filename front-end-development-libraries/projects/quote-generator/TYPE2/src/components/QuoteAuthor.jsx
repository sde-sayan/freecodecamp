const QuoteAuthor = (props) => {
    return (
        <div className="author">
            <p className="author-name" id="author">{ props.author }</p>
        </div>
    )
}

export default QuoteAuthor;
